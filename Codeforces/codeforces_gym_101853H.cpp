#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, surf;
    cin >> t;
    while(t--)
    {
        cin >> n;
        surf = sqrt(n/6);
        cout << surf << endl;
    }
    return 0;
}
